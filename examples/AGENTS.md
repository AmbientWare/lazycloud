# Examples

Show small runnable workflows using only current public SDK imports, decorators,
clients, sessions, and CLI surfaces. Keep examples aligned with docs; fix stale
APIs rather than hiding them with skips or backend imports.
