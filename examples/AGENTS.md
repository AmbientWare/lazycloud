# Examples

Small runnable workflows built from the current public SDK and CLI surface
only—the decorators, clients, sessions, and commands a user actually has.

Keep examples aligned with the documentation that references them. When an API
changes, fix the example. Do not hide the breakage behind a skip or reach past
the public surface into a backend import to keep it running: an example that
needs something a user cannot import is not an example.
