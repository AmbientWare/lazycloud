name: lazycloud

x-process-env: &process-env
  LAZYCLOUD_HOME: "${LAZYCLOUD_COMPOSE_CONTAINER_HOME:-/var/lib/lazycloud}"
  LAZYCLOUD_LOG_LEVEL: "${LAZYCLOUD_LOG_LEVEL:-INFO}"

# The host and port are literal, not `${VAR:-default}`. They are a fact of this
# file's own topology: inside the stack the database is `postgres` and the cache
# is `redis`. The same names in `.env` carry the host-side addresses that
# `lazycloud-admin` uses to reach the same services, and a passthrough would hand
# those to the containers, where `localhost` is the container itself. The
# password is not topology, and is the one Postgres itself is initialized with,
# so it reads the same variable rather than a copy that can disagree with it.
x-database-env: &database-env
  # The default names the Compose service; a deployment supplies its own, because
  # the postgres container is not among the services it starts. Deliberately not
  # named LAZYCLOUD_DATABASE_URL: that one is the host-side value the CLI uses and
  # points at localhost, which resolves to the wrong thing inside a container.
  LAZYCLOUD_DATABASE_URL: "${LAZYCLOUD_COMPOSE_DATABASE_URL:-postgresql+psycopg://lazycloud:${POSTGRES_PASSWORD:-lazycloud}@postgres:5432/lazycloud}"
x-redis-env: &redis-env
  LAZYCLOUD_REDIS_URL: "redis://redis:6379/0"

# Metrics and traces are counted in each process and pushed to the collector on
# an interval. Nothing durable holds them, so a stack without a receiver records
# into instruments that go nowhere rather than failing.
x-telemetry-env: &telemetry-env
  LAZYCLOUD_TELEMETRY_ENABLED: "${LAZYCLOUD_TELEMETRY_ENABLED:-true}"
  LAZYCLOUD_TELEMETRY_ENDPOINT: "${LAZYCLOUD_TELEMETRY_ENDPOINT:-http://otel-collector:4318}"
x-gateway-env: &gateway-env
  LAZYCLOUD_GATEWAY_PUBLIC_HTTP_URL: "${LAZYCLOUD_GATEWAY_PUBLIC_HTTP_URL:-http://localhost:${LAZYCLOUD_COMPOSE_CONTROL_PLANE_PORT:-8000}}"
  LAZYCLOUD_GATEWAY_RUNTIME_HTTP_URL: "${LAZYCLOUD_GATEWAY_RUNTIME_HTTP_URL:-http://control-plane:9000}"

x-pangolin-env: &pangolin-env
  LAZYCLOUD_PANGOLIN_API_URL: "${LAZYCLOUD_PANGOLIN_API_URL:-http://pangolin:3003/v1}"
  LAZYCLOUD_PANGOLIN_API_KEY: "${LAZYCLOUD_PANGOLIN_API_KEY:-}"
  LAZYCLOUD_PANGOLIN_ORGANIZATION_ID: "${LAZYCLOUD_PANGOLIN_ORGANIZATION_ID:-}"
  LAZYCLOUD_PANGOLIN_ENDPOINT: "${LAZYCLOUD_PANGOLIN_ENDPOINT:-http://pangolin:3000}"
  LAZYCLOUD_PANGOLIN_PLATFORM_IDENTITY_PREFIX: "${LAZYCLOUD_PANGOLIN_PLATFORM_IDENTITY_PREFIX:-lazycloud-platform}"
  LAZYCLOUD_PANGOLIN_PLATFORM_TARGET_HOST: "control-plane"
  LAZYCLOUD_PANGOLIN_PLATFORM_TARGET_PORT: "9000"

# The payment provider customers are registered with. Empty by default: the stack
# boots and goes on metering and pricing usage, but nothing can be registered or
# charged until this is set, and both places that need it name the missing
# variable rather than going quiet — sign-in registers the customer before it
# mints a session, and the scheduler sweep that delivers priced usage says so
# every tick.
x-stripe-env: &stripe-env
  LAZYCLOUD_STRIPE_API_KEY: "${LAZYCLOUD_STRIPE_API_KEY:-}"
  LAZYCLOUD_STRIPE_WEBHOOK_SECRET: "${LAZYCLOUD_STRIPE_WEBHOOK_SECRET:-}"

# The GitHub App people sign in through. Empty by default: the stack boots and
# every Compose step drives the platform with LAZYCLOUD_TOKEN, so only browser
# sign-in is unavailable, and it says which variables are missing when tried.
x-github-env: &github-env
  LAZYCLOUD_GITHUB_CLIENT_ID: "${LAZYCLOUD_GITHUB_CLIENT_ID:-}"
  LAZYCLOUD_GITHUB_CLIENT_SECRET: "${LAZYCLOUD_GITHUB_CLIENT_SECRET:-}"
  LAZYCLOUD_GITHUB_REDIRECT_URI: "${LAZYCLOUD_GITHUB_REDIRECT_URI:-}"

x-api-env: &api-env
  LAZYCLOUD_TCP_INGRESS_ENABLED: "true"
  LAZYCLOUD_TCP_INGRESS_CERTIFICATE_FILE: "/var/lib/lazycloud/tcp-ingress/tls.crt"
  LAZYCLOUD_TCP_INGRESS_KEY_FILE: "/var/lib/lazycloud/tcp-ingress/tls.key"
  LAZYCLOUD_PUBLIC_INGRESS_CLIENT_IP_HEADER: "${LAZYCLOUD_PUBLIC_INGRESS_CLIENT_IP_HEADER:-}"

x-object-store-env: &object-store-env
  LAZYCLOUD_OBJECT_STORE_BUCKET: "${LAZYCLOUD_OBJECT_STORE_BUCKET:-lazycloud-objects}"
  # Names the bucket each workspace gets. A deployment sharing an AWS account with
  # another one must differ here, because the grant that reaches `workspace-*`
  # cannot otherwise tell two deployments' customers apart.
  LAZYCLOUD_OBJECT_STORE_WORKSPACE_BUCKET_PREFIX: "${LAZYCLOUD_OBJECT_STORE_WORKSPACE_BUCKET_PREFIX:-workspace}"
  # Empty is meaningful and distinct from omitted: a connected-cloud deployment
  # sets these to "" so the object store resolves the platform role through the
  # SDK credential chain and presigns against the storage endpoint itself.
  # Use "${VAR-default}" (not ":-") so an explicit empty value survives.
  LAZYCLOUD_OBJECT_STORE_ENDPOINT_URL: "${LAZYCLOUD_OBJECT_STORE_ENDPOINT_URL-http://object-store:9000}"
  LAZYCLOUD_OBJECT_STORE_PRESIGNED_ENDPOINT_URL: "${LAZYCLOUD_OBJECT_STORE_PRESIGNED_ENDPOINT_URL-http://object-store.localhost:${LAZYCLOUD_COMPOSE_OBJECT_STORE_PORT:-9002}}"
  LAZYCLOUD_OBJECT_STORE_REGION_NAME: "${LAZYCLOUD_OBJECT_STORE_REGION_NAME:-us-east-1}"
  LAZYCLOUD_OBJECT_STORE_ACCESS_KEY_ID: "${LAZYCLOUD_OBJECT_STORE_ACCESS_KEY_ID-lazycloud-local}"
  LAZYCLOUD_OBJECT_STORE_SECRET_ACCESS_KEY: "${LAZYCLOUD_OBJECT_STORE_SECRET_ACCESS_KEY-lazycloud-local-secret}"
  LAZYCLOUD_OBJECT_STORE_FORCE_PATH_STYLE: "${LAZYCLOUD_OBJECT_STORE_FORCE_PATH_STYLE:-true}"

# Which store cuts a workspace's storage credential, and how to reach its admin
# API. Only the control plane mints; a worker is handed the result.
x-workspace-storage-env: &workspace-storage-env
  LAZYCLOUD_WORKSPACE_STORAGE_ISSUER: "garage"
  LAZYCLOUD_GARAGE_ADMIN_ENDPOINT_URL: "http://object-store:3903"
  LAZYCLOUD_GARAGE_ADMIN_TOKEN: "${LAZYCLOUD_GARAGE_ADMIN_TOKEN:-lazycloud-local-garage-admin}"
  # Short by production standards, and deliberately: the rotation and refresh
  # this drives are the parts that only ever run once an hour in production, so
  # locally they run often enough to be seen failing.
  LAZYCLOUD_GARAGE_ADMIN_CREDENTIAL_LIFETIME_SECONDS: "900"

x-image-build-env: &image-build-env
  LAZYCLOUD_IMAGE_BUILD_CONTAINER_ADDRESS_WAIT_TIMEOUT_SECONDS: "120"
  LAZYCLOUD_IMAGE_BUILD_CONTAINER_ADDRESS_POLL_INTERVAL_SECONDS: "0.5"
  # Empty means the workspace compute policy chooses where builds run. Pinning a
  # pool here attaches every build to that pool and silently overrides the policy,
  # so a deployment whose capacity is a connected provider queues each build for
  # local capacity that does not exist until it hits the retry limit.

# The release a deployment points at publishes the agent artifact version and
# digest, the URL serving it, the container-worker image, the customer
# authorization template, and the baked CPU AMIs. Every process that composes a
# pool bootstrap needs the same one.
x-release-env: &release-env
  LAZYCLOUD_RELEASE_MANIFEST_URL: "${LAZYCLOUD_RELEASE_MANIFEST_URL:-}"

x-agent-binary-env: &agent-binary-env
  LAZYCLOUD_AGENT_BINARY_DIR: "${LAZYCLOUD_AGENT_BINARY_DIR:-/var/lib/lazycloud/agent-binarys}"

x-aws-compute-env: &aws-compute-env
  LAZYCLOUD_AWS_CONNECTION_CONTROL_PRINCIPAL_ARN: "${LAZYCLOUD_AWS_CONNECTION_CONTROL_PRINCIPAL_ARN:-}"
  LAZYCLOUD_AWS_CAPACITY_INSTANCE_HOURLY_MICROS: "${LAZYCLOUD_AWS_CAPACITY_INSTANCE_HOURLY_MICROS:-{}}"

x-scheduler-env: &scheduler-env
  LAZYCLOUD_COMPUTE_RECLAIM_REGISTRATION_DEADLINE_SECONDS: "3600"

x-platform-aws-credentials: &platform-aws-credentials
  AWS_REGION: "${AWS_REGION:-us-east-1}"
  AWS_DEFAULT_REGION: "${AWS_DEFAULT_REGION:-us-east-1}"
  AWS_CONFIG_FILE: /run/lazycloud/aws/config
  AWS_SHARED_CREDENTIALS_FILE: /run/lazycloud/aws/credentials
  AWS_SDK_LOAD_CONFIG: "1"
  # Not AWS_PROFILE: boto applies that to every client in the process, and only
  # calls into a connected account belong on the control principal.
  LAZYCLOUD_AWS_CONNECTION_PROFILE: "${LAZYCLOUD_COMPOSE_AWS_PROFILE:-control}"

x-platform-aws-config: &platform-aws-config
  "${LAZYCLOUD_COMPOSE_AWS_CONFIG_DIR:-$HOME/.lazycloud/compose-aws}:/run/lazycloud/aws:ro"

x-backend-route-credentials: &backend-route-credentials
  LAZYCLOUD_BACKEND_ROUTE_AUTH_KEY: "${LAZYCLOUD_BACKEND_ROUTE_AUTH_KEY:-}"

x-container-service-credentials: &container-service-credentials
  LAZYCLOUD_CONTAINER_SERVICE_TOKEN: "${LAZYCLOUD_CONTAINER_SERVICE_TOKEN:-}"

x-cache-origin-registry: &cache-origin-registry
  LAZYCLOUD_IMAGE_REGISTRY_STORE: "s3"
  LAZYCLOUD_BUILD_REGISTRY: "${LAZYCLOUD_BUILD_REGISTRY:-}"
  LAZYCLOUD_BUILD_REGISTRY_CREDENTIALS: "${LAZYCLOUD_BUILD_REGISTRY_CREDENTIALS:-}"
  LAZYCLOUD_BUILD_REGISTRY_CREDENTIALS_SECRET: "${LAZYCLOUD_BUILD_REGISTRY_CREDENTIALS_SECRET:-}"

x-database-bootstrap-env: &database-bootstrap-env
  LAZYCLOUD_HOME: "${LAZYCLOUD_COMPOSE_CONTAINER_HOME:-/var/lib/lazycloud}"
  LAZYCLOUD_LOG_LEVEL: "${LAZYCLOUD_LOG_LEVEL:-INFO}"
  # The default names the Compose service; a deployment supplies its own, because
  # the postgres container is not among the services it starts. Deliberately not
  # named LAZYCLOUD_DATABASE_URL: that one is the host-side value the CLI uses and
  # points at localhost, which resolves to the wrong thing inside a container.
  LAZYCLOUD_DATABASE_URL: "${LAZYCLOUD_COMPOSE_DATABASE_URL:-postgresql+psycopg://lazycloud:${POSTGRES_PASSWORD:-lazycloud}@postgres:5432/lazycloud}"

x-lazycloud-build-args: &lazycloud-build-args
  # Where the examples and the docs live, baked into the web bundle at build
  # time. Empty by default, and empty means the marketing pages render no entry
  # point for them rather than one that goes nowhere.
  VITE_LAZYCLOUD_EXAMPLES_URL: "${LAZYCLOUD_EXAMPLES_URL:-}"
  VITE_LAZYCLOUD_DOCS_URL: "${LAZYCLOUD_DOCS_URL:-}"

x-container-worker-settings: &container-worker-settings
  restart: unless-stopped
  privileged: true
  cgroup: host
  # The slot a deployed worker gets from the agent, so a local one is bounded the
  # same way. Without it the worker's cgroup is unlimited, its memory pressure
  # describes the whole development machine rather than its own share, and the
  # eviction watcher correctly refuses to run -- which means the path is never
  # exercised anywhere a person would notice it breaking.
  mem_limit: "${LAZYCLOUD_WORKER_MEMORY_LIMIT:-4g}"
  cap_add:
    - NET_ADMIN
    - SYS_ADMIN
    - SYS_PTRACE
    - MKNOD
  security_opt:
    - seccomp=unconfined
    - apparmor=unconfined
  devices:
    - /dev/fuse:/dev/fuse
  tmpfs:
    - /tmp:exec
    - /run
    - /run/gvisor
    - /dev/shm:rw,exec,nosuid,nodev,size=1g
  stop_grace_period: 90s
  ulimits:
    nofile:
      soft: 1048576
      hard: 1048576
  entrypoint:
    - /bin/sh
    - -ec
    - |
      # The worker's containers live behind its own bridge and cannot resolve a
      # Compose service name, so the in-stack origin is rewritten to the address the
      # daemon routes to. WORKER_REPOSITORY_URL and GATEWAY_HTTP_URL must stay the
      # same string; when they diverged the worker never reported itself available.
      if [ "$${WORKER_REPOSITORY_URL:-}" = "http://control-plane:9000" ]; then
        compose_gateway="$$(ip -4 route show default | awk '/default/ {print $$3; exit}')"
        if [ -z "$$compose_gateway" ]; then
          echo "Docker gateway is required for the public worker repository URL" >&2
          exit 2
        fi
        export WORKER_REPOSITORY_URL="http://$${compose_gateway}:$${LAZYCLOUD_COMPOSE_CONTROL_PLANE_PORT:-8000}"
        export GATEWAY_HTTP_URL="$$WORKER_REPOSITORY_URL"
      fi
      if [ -z "$${WORKER_TOKEN:-}" ] && [ -s /var/lib/lazycloud/tokens/worker-token ]; then
        export WORKER_TOKEN="$$(cat /var/lib/lazycloud/tokens/worker-token)"
      fi
      # Registration is refused unless the worker names a unit that feeds its pool,
      # and the id is assigned when that unit is created rather than derived.
      if [ -z "$${WORKER_CAPACITY_OWNER_ID:-}" ] && [ -s /var/lib/lazycloud/tokens/worker-capacity-owner ]; then
        export WORKER_CAPACITY_OWNER_ID="$$(cat /var/lib/lazycloud/tokens/worker-capacity-owner)"
      fi
      if [ -z "$${WORKER_TOKEN:-}" ] || [ -z "$${WORKER_CAPACITY_OWNER_ID:-}" ]; then
        echo "WORKER_TOKEN and WORKER_CAPACITY_OWNER_ID are required" >&2
        exit 2
      fi
      exec /usr/bin/tini -- "$$0" "$$@"

x-container-worker-healthcheck: &container-worker-healthcheck
  test:
    [
      "CMD",
      "python",
      "-m",
      "shared.process_liveness",
      "/tmp/lazycloud-container-worker.heartbeat",
      "--max-age-seconds",
      "45",
    ]
  interval: 10s
  timeout: 5s
  retries: 12
  start_period: 120s

x-lazycloud-depends: &lazycloud-depends
  postgres:
    condition: service_healthy
  database-bootstrap:
    condition: service_completed_successfully
  redis:
    condition: service_healthy
  object-store:
    condition: service_healthy
  object-store-bucket:
    condition: service_completed_successfully
  pangolin-bootstrap:
    condition: service_completed_successfully

x-lazycloud-role-depends: &lazycloud-role-depends
  control-plane:
    condition: service_healthy

services:
  # No health check: the image carries no shell and no HTTP client, so there is
  # nothing to exec. Nothing depends on this service either — a process whose
  # exporter cannot reach a receiver drops the batch and keeps serving.
  otel-collector:
    image: otel/opentelemetry-collector:0.119.0
    restart: unless-stopped
    command: ["--config=/etc/otel/config.yaml"]
    volumes:
      - ./deploy/telemetry/collector.yaml:/etc/otel/config.yaml:ro

  postgres:
    image: postgres:18-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: "lazycloud"
      POSTGRES_USER: "lazycloud"
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD:-lazycloud}"
    ports:
      - "${LAZYCLOUD_COMPOSE_POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres-data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U lazycloud -d lazycloud"]
      interval: 5s
      timeout: 3s
      retries: 20

  pangolin-postgres:
    image: postgres:18-alpine@sha256:d3e1620b530c944afa6e887d22eb899824da68e19c52024bf98f5220c88a65b2
    restart: unless-stopped
    environment:
      POSTGRES_DB: pangolin
      POSTGRES_USER: pangolin
      POSTGRES_PASSWORD: "${LAZYCLOUD_PANGOLIN_POSTGRES_PASSWORD:?set a local Pangolin PostgreSQL password}"
    volumes:
      - pangolin-postgres-data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pangolin -d pangolin"]
      interval: 5s
      timeout: 3s
      retries: 20

  pangolin:
    image: docker.io/fosrl/pangolin:ee-postgresql-1.21.1@sha256:8de423e4ae1127db8a0ae20a8032ef08456f798bc7721b7fbe42d5e8807fa9a8
    restart: unless-stopped
    environment:
      SERVER_SECRET: "${LAZYCLOUD_PANGOLIN_SERVER_SECRET:?set a local Pangolin server secret}"
      POSTGRES_CONNECTION_STRING: "postgresql://pangolin:${LAZYCLOUD_PANGOLIN_POSTGRES_PASSWORD:?set a local Pangolin PostgreSQL password}@pangolin-postgres:5432/pangolin?sslmode=disable"
    depends_on:
      pangolin-postgres:
        condition: service_healthy
    configs:
      - source: pangolin-configuration
        target: /app/config/config.yml
        mode: 0444
    healthcheck:
      test: ["CMD", "curl", "-f", "http://127.0.0.1:3001/api/v1/"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 20s

  gerbil:
    image: docker.io/fosrl/gerbil:1.3.1@sha256:b16a722d5603fa74acd3e1deb86cea0b8330d015ab5325dc41e44108fe6f29c9
    restart: unless-stopped
    command:
      - --reachableAt=http://gerbil:3004
      - --generateAndSaveKeyTo=/var/config/key
      - --remoteConfig=http://pangolin:3001/api/v1/
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    ports:
      - "${LAZYCLOUD_COMPOSE_PANGOLIN_HTTP_PORT:-8080}:80"
      - "${LAZYCLOUD_COMPOSE_PANGOLIN_HTTPS_PORT:-8443}:443"
      - "${LAZYCLOUD_COMPOSE_PANGOLIN_HTTPS_PORT:-8443}:443/udp"
      - "${LAZYCLOUD_COMPOSE_PANGOLIN_SITE_PORT:-51820}:51820/udp"
      - "${LAZYCLOUD_COMPOSE_PANGOLIN_CLIENT_PORT:-21820}:21820/udp"
    depends_on:
      pangolin:
        condition: service_healthy
    volumes:
      - pangolin-gerbil-config:/var/config
    healthcheck:
      test: ["CMD-SHELL", "test -s /var/config/key"]
      interval: 5s
      timeout: 3s
      retries: 24
      start_period: 10s

  pangolin-traefik:
    image: docker.io/traefik:v3.7.0@sha256:eb328e2c806c53aafbbace6c451fa54d268961261a85452fcf0fb752a30c17be
    restart: unless-stopped
    network_mode: service:gerbil
    command: ["--configFile=/etc/traefik/traefik_config.yml"]
    depends_on:
      pangolin:
        condition: service_healthy
      gerbil:
        condition: service_healthy
    configs:
      - source: pangolin-traefik-configuration
        target: /etc/traefik/traefik_config.yml
        mode: 0444
      - source: pangolin-traefik-dynamic-configuration
        target: /etc/traefik/dynamic_config.yml
        mode: 0444
    healthcheck:
      test:
        [
          "CMD",
          "traefik",
          "healthcheck",
          "--ping",
          "--configFile=/etc/traefik/traefik_config.yml",
        ]
      interval: 5s
      timeout: 3s
      retries: 24
      start_period: 10s

  pangolin-bootstrap:
    image: cli:local
    build:
      context: .
      dockerfile: docker/Dockerfile.control-plane
      target: cli
      args: *lazycloud-build-args
    command:
      - lazycloud-admin
      - --json
      - pangolin
      - bootstrap-filesystem
      - --directory
      - /var/lib/lazycloud/pangolin
      - --public-hostname
      - lazycloud.localhost
    environment: *pangolin-env
    depends_on:
      pangolin:
        condition: service_healthy
      pangolin-traefik:
        condition: service_healthy
    volumes:
      - pangolin-runtime:/var/lib/lazycloud/pangolin

  pangolin-site:
    image: docker.io/fosrl/newt:1.16.0@sha256:345fdeb369be6608d82c41d70637636c78b2c04a6112ff6ec20fc21c48afc899
    restart: unless-stopped
    entrypoint: ["/bin/sh", "-ec"]
    command:
      - |
        export NEWT_ID="$$(cat /var/lib/lazycloud/pangolin/LAZYCLOUD_PANGOLIN_PLATFORM_CONNECTOR_0_ID)"
        export NEWT_SECRET="$$(cat /var/lib/lazycloud/pangolin/LAZYCLOUD_PANGOLIN_PLATFORM_CONNECTOR_0_SECRET)"
        exec newt
    environment:
      PANGOLIN_ENDPOINT: "${LAZYCLOUD_PANGOLIN_ENDPOINT:-http://pangolin:3000}"
      DISABLE_CLIENTS: "true"
      HEALTH_FILE: /tmp/healthy
    depends_on:
      pangolin-bootstrap:
        condition: service_completed_successfully
    volumes:
      - pangolin-runtime:/var/lib/lazycloud/pangolin:ro
    healthcheck:
      test: ["CMD-SHELL", "test -f /tmp/healthy"]
      interval: 5s
      timeout: 3s
      retries: 24
      start_period: 10s

  database-bootstrap:
    image: database-bootstrap:local
    build:
      context: .
      dockerfile: docker/Dockerfile.control-plane
      target: database-bootstrap
      args: *lazycloud-build-args
    command: ["lazycloud-admin", "database", "initialize"]
    environment: *database-bootstrap-env
    depends_on:
      postgres:
        condition: service_healthy

  administrator-bootstrap:
    image: database-bootstrap:local
    entrypoint: ["/bin/sh", "-ec"]
    command:
      - |
        token_path=/var/lib/lazycloud/bootstrap/administrator-token
        umask 077
        # Empty expands to no flag at all, so an unset id leaves a token-only
        # administrator rather than linking one nobody named.
        github_id="${LAZYCLOUD_ADMINISTRATOR_GITHUB_USER_ID:-}"
        lazycloud-admin --json auth bootstrap \
          --token-file /run/secrets/lazycloud-administrator-token \
          $${github_id:+--github-user-id "$$github_id"} \
          --output "$$token_path"
        if test -e "$$token_path"; then
          test "$$(stat -c '%a' "$$token_path")" = "600"
        fi
    environment:
      <<: [*database-bootstrap-env, *object-store-env]
      LAZYCLOUD_ADMINISTRATOR_GITHUB_USER_ID: "${LAZYCLOUD_ADMINISTRATOR_GITHUB_USER_ID:-}"
    depends_on:
      database-bootstrap:
        condition: service_completed_successfully
      object-store-bucket:
        condition: service_completed_successfully
    volumes:
      - lazycloud-administrator-token:/var/lib/lazycloud/bootstrap
    secrets:
      - source: lazycloud-bootstrap-administrator-token
        target: lazycloud-administrator-token
        mode: 0400

  redis:
    image: redis:8-alpine
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "${LAZYCLOUD_COMPOSE_REDIS_PORT:-6379}:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20

  object-store:
    image: dxflrs/garage:v2.3.0
    restart: unless-stopped
    command: ["/garage", "server", "--single-node", "--default-bucket"]
    environment:
      GARAGE_DEFAULT_BUCKET: "lazycloud-objects"
      GARAGE_DEFAULT_ACCESS_KEY: "lazycloud-local"
      GARAGE_DEFAULT_SECRET_KEY: "lazycloud-local-secret"
    ports:
      - "${LAZYCLOUD_COMPOSE_OBJECT_STORE_PORT:-9002}:9000"
      - "${LAZYCLOUD_COMPOSE_OBJECT_STORE_ADMIN_PORT:-3903}:3903"
    volumes:
      - ./deploy/garage/garage.toml:/etc/garage.toml:ro
      - object-store-data:/var/lib/garage
    healthcheck:
      test: ["CMD", "/garage", "status"]
      interval: 5s
      timeout: 3s
      retries: 20

  object-store-bucket:
    image: amazon/aws-cli:2.28.3
    depends_on:
      object-store:
        condition: service_healthy
    environment:
      AWS_ACCESS_KEY_ID: "lazycloud-local"
      AWS_SECRET_ACCESS_KEY: "lazycloud-local-secret"
      AWS_DEFAULT_REGION: "us-east-1"
      AWS_EC2_METADATA_DISABLED: "true"
      GARAGE_ENDPOINT_URL: "http://object-store:9000"
    entrypoint: ["/bin/sh", "-ec"]
    command:
      - |
        for bucket in lazycloud-objects default lazycloud-data lazycloud-source-packages; do
          if aws --endpoint-url "$$GARAGE_ENDPOINT_URL" s3api head-bucket --bucket "$$bucket" >/dev/null 2>&1; then
            continue
          fi
          aws --endpoint-url "$$GARAGE_ENDPOINT_URL" s3api create-bucket --bucket "$$bucket" \
            || aws --endpoint-url "$$GARAGE_ENDPOINT_URL" s3api head-bucket --bucket "$$bucket" >/dev/null
        done

  tcp-certificate:
    image: api:local
    command:
      [
        "lazycloud-tcp-certificate",
        "--certificate-file",
        "/var/lib/lazycloud/tcp-ingress/tls.crt",
        "--key-file",
        "/var/lib/lazycloud/tcp-ingress/tls.key",
        "--external-host",
        "${LAZYCLOUD_TCP_INGRESS_EXTERNAL_HOST:-tcp.localhost}",
      ]
    volumes:
      - tcp-ingress-tls:/var/lib/lazycloud/tcp-ingress

  control-plane:
    image: api:local
    restart: unless-stopped
    build:
      context: .
      dockerfile: docker/Dockerfile.control-plane
      target: api
      args: *lazycloud-build-args
    entrypoint: ["/bin/sh", "-ec"]
    command:
      - |
        export LAZYCLOUD_PANGOLIN_PLATFORM_SITE_IDS="$$(cat /var/lib/lazycloud/pangolin/LAZYCLOUD_PANGOLIN_PLATFORM_SITE_IDS)"
        export LAZYCLOUD_PANGOLIN_PLATFORM_CLIENT_RECORD_IDS="$$(cat /var/lib/lazycloud/pangolin/LAZYCLOUD_PANGOLIN_PLATFORM_CLIENT_RECORD_IDS)"
        exec lazycloud-api --host 0.0.0.0 --port 9000 --log-level "${LAZYCLOUD_LOG_LEVEL:-INFO}"
    environment:
      <<:
        [
          *process-env,
          *database-env,
          *redis-env,
          *telemetry-env,
          *gateway-env,
          *pangolin-env,
          *stripe-env,
          *github-env,
          *api-env,
          *object-store-env,
          *workspace-storage-env,
          *image-build-env,
          *release-env,
          *agent-binary-env,
          *aws-compute-env,
          *platform-aws-credentials,
          *backend-route-credentials,
          *container-service-credentials,
          *cache-origin-registry,
        ]
    # One here because this local service publishes fixed host ports. Production
    # replicas sit behind Pangolin's platform resource.
    deploy:
      replicas: 1
    ports:
      - "${LAZYCLOUD_COMPOSE_CONTROL_PLANE_PORT:-8000}:9000"
      - "${LAZYCLOUD_COMPOSE_TCP_INGRESS_PORT:-1995}:1995"
    depends_on:
      <<: *lazycloud-depends
      administrator-bootstrap:
        condition: service_completed_successfully
      tcp-certificate:
        condition: service_completed_successfully
    volumes:
      - lazycloud-files:/var/lib/lazycloud
      - "${LAZYCLOUD_COMPOSE_AGENT_BINARY_DIR:-./.lazycloud/agent-binarys}:/var/lib/lazycloud/agent-binarys:ro"
      - tcp-ingress-tls:/var/lib/lazycloud/tcp-ingress:ro
      - pangolin-runtime:/var/lib/lazycloud/pangolin:ro
      - *platform-aws-config
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "from urllib.request import urlopen; urlopen('http://127.0.0.1:9000/health', timeout=3).read()",
        ]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s

  pangolin-client:
    image: docker.io/fosrl/pangolin-cli:0.16.0@sha256:a25a6d81b1f6c20f9f4c47fda4c82e61ec5a7cf72f6465174db552b0fe616434
    restart: unless-stopped
    network_mode: service:control-plane
    entrypoint: ["/bin/sh", "-ec"]
    command:
      - |
        export CLIENT_ID="$$(cat /var/lib/lazycloud/pangolin/LAZYCLOUD_PANGOLIN_PLATFORM_CLIENT_0_ID)"
        export CLIENT_SECRET="$$(cat /var/lib/lazycloud/pangolin/LAZYCLOUD_PANGOLIN_PLATFORM_CLIENT_0_SECRET)"
        exec /entrypoint.sh
    environment:
      PANGOLIN_ENDPOINT: "${LAZYCLOUD_PANGOLIN_ENDPOINT:-http://pangolin:3000}"
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    depends_on:
      control-plane:
        condition: service_healthy
      pangolin-bootstrap:
        condition: service_completed_successfully
    volumes:
      - pangolin-runtime:/var/lib/lazycloud/pangolin:ro
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "status=$$(pangolin-cli status client --json) && printf '%s\\n' \"$$status\" | grep -q '\"connected\": true' && printf '%s\\n' \"$$status\" | grep -q '\"registered\": true'",
        ]
      interval: 5s
      timeout: 5s
      retries: 24
      start_period: 10s

  cli:
    profiles: ["tools"]
    image: cli:local
    build:
      context: .
      dockerfile: docker/Dockerfile.control-plane
      target: cli
      args: *lazycloud-build-args
    command: ["lazycloud-admin", "--help"]
    environment:
      <<:
        [
          *process-env,
          *database-env,
          *redis-env,
          *gateway-env,
          *pangolin-env,
          *object-store-env,
          *image-build-env,
          *agent-binary-env,
          *backend-route-credentials,
          *container-service-credentials,
        ]
    depends_on:
      control-plane:
        condition: service_healthy
    volumes:
      - lazycloud-files:/var/lib/lazycloud

  agent-join-token:
    image: cli:local
    build:
      context: .
      dockerfile: docker/Dockerfile.control-plane
      target: cli
      args: *lazycloud-build-args
    entrypoint: ["/bin/sh", "-ec"]
    command:
      - |
        state_dir="${LAZYCLOUD_COMPOSE_AGENT_STATE_DIR:-/var/lib/lazycloud/agent}"
        token_path="$$state_dir/join-token"
        mkdir -p "$$state_dir"
        chmod 0700 "$$state_dir"
        export GATEWAY_TOKEN="$$(cat /run/secrets/lazycloud-administrator-token)"
        # The credential belongs to the customer even though the administrator mints
        # it: the enrollment names whoever asked, and placement compares that account
        # against the one the work belongs to. An administrator-owned token would make
        # the machine the platform's and prove nothing about tenancy.
        customer_name="${LAZYCLOUD_COMPOSE_CUSTOMER_NAME:-customer}"
        customer_id="$$(lazycloud-admin --json user list \
          | python -c 'import json,sys; n=sys.argv[1]; print(next((u["id"] for u in json.load(sys.stdin) if u["display_name"]==n), ""))' \
              "$$customer_name")"
        if [ -z "$$customer_id" ]; then
          customer_id="$$(lazycloud-admin --json user create --name "$$customer_name" --administrator \
            | python -c 'import json,sys; print(json.load(sys.stdin)["id"])')"
        fi
        customer_token="$$(lazycloud-admin --json token create compose-agent-join --user "$$customer_id" \
          | python -c 'import json,sys; print(json.load(sys.stdin)["token"])')"
        export GATEWAY_TOKEN="$$customer_token"
        lazycloud-admin --json workspace show "${LAZYCLOUD_COMPOSE_CUSTOMER_WORKSPACE:-tenant-customer}" >/dev/null 2>&1 \
          || lazycloud-admin --json workspace create "${LAZYCLOUD_COMPOSE_CUSTOMER_WORKSPACE:-tenant-customer}" >/dev/null
        lazycloud-admin --json compute policy update \
          --workspace "${LAZYCLOUD_COMPOSE_CUSTOMER_WORKSPACE:-tenant-customer}" \
          --default-pool "${LAZYCLOUD_COMPOSE_SELF_HOSTED_POOL:-self-hosted}" >/dev/null
        temporary="$${token_path}.tmp"
        rm -f "$$temporary"
        # Retried, because health is a lagging signal: the ingress reports what
        # was true at its last probe, and a replica recreated since then refuses
        # until it is listening. One attempt turns that window into a stack with
        # no join credential and an agent that never arrives.
        attempt=1
        until lazycloud-admin --json machine join-token --pool "${LAZYCLOUD_COMPOSE_SELF_HOSTED_POOL:-self-hosted}" --ttl 15m \
          | python -c \
              'import json, pathlib, sys; pathlib.Path(sys.argv[1]).write_text(json.load(sys.stdin)["token"], encoding="utf-8")' \
              "$$temporary"
        do
          if [ "$$attempt" -ge 30 ]; then
            echo "no control plane issued a join credential after $$attempt attempts" >&2
            exit 1
          fi
          attempt=$$((attempt + 1))
          sleep 2
        done
        chmod 0600 "$$temporary"
        mv "$$temporary" "$$token_path"
        echo "compose agent one-use join credential initialized"
    environment:
      GATEWAY_HTTP_URL: "http://control-plane:9000"
      WORKSPACE_ID: "${LAZYCLOUD_COMPOSE_CUSTOMER_WORKSPACE:-tenant-customer}"
    depends_on:
      control-plane:
        condition: service_healthy
    volumes:
      - "${LAZYCLOUD_COMPOSE_AGENT_STATE_DIR:-/var/lib/lazycloud/agent}:${LAZYCLOUD_COMPOSE_AGENT_STATE_DIR:-/var/lib/lazycloud/agent}"
    secrets:
      - source: lazycloud-bootstrap-administrator-token
        target: lazycloud-administrator-token
        mode: 0400

  agent:
    image: agent:local
    restart: unless-stopped
    init: true
    privileged: true
    build:
      context: .
      dockerfile: deploy/agent-binary/Dockerfile
      target: agent-runtime
    entrypoint: ["/bin/sh", "-ec"]
    command:
      - |
        compose_gateway="$$(ip -4 route show default | awk '/default/ {print $$3; exit}')"
        if [ -z "$$compose_gateway" ]; then
          echo "Docker gateway is required for the agent-managed worker" >&2
          exit 2
        fi
        gateway_url="http://$${compose_gateway}:${LAZYCLOUD_COMPOSE_CONTROL_PLANE_PORT:-8000}"
        exec lazycloud-agent join \
          --gateway "$$gateway_url" \
          --join-token-file "${LAZYCLOUD_COMPOSE_AGENT_STATE_DIR:-/var/lib/lazycloud/agent}/join-token" \
          --state-dir "${LAZYCLOUD_COMPOSE_AGENT_STATE_DIR:-/var/lib/lazycloud/agent}" \
          --hostname "${LAZYCLOUD_COMPOSE_AGENT_HOSTNAME:-compose-agent}" \
          --machine-fingerprint "${LAZYCLOUD_COMPOSE_AGENT_MACHINE_FINGERPRINT:-compose-agent}" \
          --worker-image "container-worker:local" \
          --worker-route-target "$$compose_gateway" \
          --worker-runtime-http-url "$$gateway_url" \
          --worker-network "host" \
          --worker-bridge-name "${LAZYCLOUD_COMPOSE_AGENT_WORKER_BRIDGE:-rt_br0}" \
          --worker-bridge-subnet "${LAZYCLOUD_COMPOSE_AGENT_WORKER_SUBNET:-192.168.0.0/20}" \
          --worker-bridge-ipv6-subnet "${LAZYCLOUD_COMPOSE_AGENT_WORKER_IPV6_SUBNET:-fd00:abcd::/64}" \
          --worker-host-alias "object-store.localhost:host-gateway" \
          --max-cpu "${LAZYCLOUD_COMPOSE_AGENT_MAX_CPU_CORES:-4}" \
          --max-memory "${LAZYCLOUD_COMPOSE_CONTAINER_WORKER_MEMORY_MIB:-8192}MiB" \
          --stream-interval-seconds "${LAZYCLOUD_COMPOSE_AGENT_STREAM_INTERVAL_SECONDS:-1}"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    devices:
      - /dev/fuse:/dev/fuse
    environment:
      # Forwarded to worker slots so they can back platform volumes. Platform
      # deployments set these; agents on customer infrastructure do not, and
      # their slots refuse volume work instead of writing to disposable disk.
      DATA_STORAGE_BUCKET: "lazycloud-data"
      DATA_STORAGE_ENDPOINT_URL: "http://object-store:9000"
      DATA_STORAGE_REGION_NAME: "us-east-1"
      DATA_STORAGE_ACCESS_KEY_ID: "lazycloud-local"
      DATA_STORAGE_SECRET_ACCESS_KEY: "${DATA_STORAGE_SECRET_ACCESS_KEY:-lazycloud-local-secret}"
      DATA_STORAGE_FORCE_PATH_STYLE: "true"
    depends_on:
      agent-join-token:
        condition: service_completed_successfully
      cache-server:
        condition: service_healthy
    volumes:
      - "${LAZYCLOUD_COMPOSE_AGENT_STATE_DIR:-/var/lib/lazycloud/agent}:${LAZYCLOUD_COMPOSE_AGENT_STATE_DIR:-/var/lib/lazycloud/agent}"
      - /var/run/docker.sock:/var/run/docker.sock
      - /var/run/netns:/var/run/netns
    healthcheck:
      test:
        [
          "CMD",
          "test",
          "-s",
          "${LAZYCLOUD_COMPOSE_AGENT_STATE_DIR:-/var/lib/lazycloud/agent}/runtime-ready.json",
        ]
      interval: 5s
      timeout: 3s
      retries: 24
      start_period: 10s

  scheduler:
    image: scheduler:local
    # Two, like the control plane. The work that spends money already
    # serialises on a Redis key per capacity owner; what needed fixing was the
    # orphan confirmation window and pod expiry, both of which decided from
    # process-local state.
    deploy:
      replicas: 2
    restart: unless-stopped
    build:
      context: .
      dockerfile: docker/Dockerfile.control-plane
      target: scheduler
      args: *lazycloud-build-args
    entrypoint: ["/bin/sh", "-ec"]
    command:
      - |
        export LAZYCLOUD_PANGOLIN_PLATFORM_SITE_IDS="$$(cat /var/lib/lazycloud/pangolin/LAZYCLOUD_PANGOLIN_PLATFORM_SITE_IDS)"
        export LAZYCLOUD_PANGOLIN_PLATFORM_CLIENT_RECORD_IDS="$$(cat /var/lib/lazycloud/pangolin/LAZYCLOUD_PANGOLIN_PLATFORM_CLIENT_RECORD_IDS)"
        exec lazycloud-scheduler --container-limit "${LAZYCLOUD_COMPOSE_SCHEDULER_CONTAINER_LIMIT:-100}"
    environment:
      <<:
        [
          *process-env,
          *database-env,
          *redis-env,
          *telemetry-env,
          *gateway-env,
          *pangolin-env,
          # The scheduler drains the meter outbox, so it holds the payment
          # credential the control plane holds. Without it this process runs and
          # never delivers, which is metered usage nobody is charged for.
          *stripe-env,
          *object-store-env,
          *release-env,
          *agent-binary-env,
          *aws-compute-env,
          *scheduler-env,
          *platform-aws-credentials,
          *backend-route-credentials,
          *container-service-credentials,
        ]
    depends_on: *lazycloud-role-depends
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-m",
          "shared.process_liveness",
          "/tmp/lazycloud-scheduler.heartbeat.placement",
          "/tmp/lazycloud-scheduler.heartbeat.capacity",
          "/tmp/lazycloud-scheduler.heartbeat.housekeeping",
          "/tmp/lazycloud-scheduler.heartbeat.dispatch",
          "--max-age-seconds",
          "${LAZYCLOUD_COMPOSE_SCHEDULER_LIVENESS_MAX_AGE_SECONDS:-300}",
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 120s
    volumes:
      - lazycloud-files:/var/lib/lazycloud
      - pangolin-runtime:/var/lib/lazycloud/pangolin:ro
      - *platform-aws-config

  platform-unit:
    # The capacity owner the shared fleet registers against. A worker is admitted
    # only into a pool some unit already feeds, and the id is assigned when the unit
    # is created rather than derived from the pool name, so it is read back here and
    # handed to the worker through a file.
    image: cli:local
    entrypoint: ["/bin/sh", "-ec"]
    command:
      - |
        export GATEWAY_TOKEN="$$(cat /run/secrets/lazycloud-administrator-token)"
        pool="${LAZYCLOUD_COMPOSE_CONTAINER_WORKER_POOL:-lazycloud}"
        owner_path=/var/lib/lazycloud/tokens/worker-capacity-owner
        mkdir -p "$$(dirname "$$owner_path")"
        umask 077
        lazycloud-admin unit ensure "$$pool-shared" \
          --pool "$$pool" --provider local --capacity-owner-output "$$owner_path"
        echo "shared fleet capacity owner ready: $$(cat "$$owner_path")"
    environment:
      GATEWAY_HTTP_URL: "http://control-plane:9000"
      LAZYCLOUD_HOME: "${LAZYCLOUD_COMPOSE_CONTAINER_HOME:-/var/lib/lazycloud}"
      WORKSPACE_ID: "default"
    depends_on:
      <<: *lazycloud-role-depends
    secrets:
      - source: lazycloud-bootstrap-administrator-token
        target: lazycloud-administrator-token
        mode: 0400
    volumes:
      - lazycloud-worker-tokens:/var/lib/lazycloud/tokens

  worker-token:
    # The shared fleet authenticates as the platform, not as a machine somebody
    # joined, so its credential is a service token rather than an agent slot token.
    # Reused when one is already published, so restarts do not mint a second.
    image: worker-bootstrap:local
    build:
      context: .
      dockerfile: docker/Dockerfile.control-plane
      target: worker-bootstrap
      args: *lazycloud-build-args
    entrypoint: ["/bin/sh", "-ec"]
    command:
      - |
        token_path=/var/lib/lazycloud/tokens/worker-token
        mkdir -p "$$(dirname "$$token_path")"
        umask 077
        lazycloud-worker-bootstrap worker-token \
          "${LAZYCLOUD_COMPOSE_CONTAINER_WORKER_ID:-compose-container-worker}" \
          --output "$$token_path"
        echo "shared fleet worker token initialized"
    environment:
      <<: [*process-env, *database-env, *redis-env]
      LAZYCLOUD_HOME: "${LAZYCLOUD_COMPOSE_CONTAINER_HOME:-/var/lib/lazycloud}"
    depends_on:
      <<: *lazycloud-depends
      administrator-bootstrap:
        condition: service_completed_successfully
    volumes:
      - lazycloud-worker-tokens:/var/lib/lazycloud/tokens

  container-worker:
    # The shared SaaS fleet: public capacity any account's work can land on, which is
    # what an unconfigured workspace gets. The agents beside it are joined machines
    # and serve only the account that connected them.
    <<: *container-worker-settings
    image: container-worker:local
    build:
      context: .
      dockerfile: docker/Dockerfile.worker
      target: container-worker
      args: *lazycloud-build-args
    extra_hosts:
      - "host.docker.internal:host-gateway"
      - "object-store.localhost:host-gateway"
    command:
      [
        "lazycloud-container-worker",
        "--worker-id",
        "${LAZYCLOUD_COMPOSE_CONTAINER_WORKER_ID:-compose-container-worker}",
        "--machine-id",
        "${LAZYCLOUD_COMPOSE_CONTAINER_WORKER_MACHINE:-compose-machine}",
        "--pool",
        "${LAZYCLOUD_COMPOSE_CONTAINER_WORKER_POOL:-lazycloud}",
        "--pod-address",
        "${LAZYCLOUD_COMPOSE_CONTAINER_WORKER_POD_ADDRESS:-container-worker}",
        "--container-service-port",
        "${LAZYCLOUD_COMPOSE_CONTAINER_WORKER_SERVICE_PORT:-8910}",
        "--checkpoint-bucket",
        "lazycloud-objects",
      ]
    environment:
      LAZYCLOUD_HOME: "${LAZYCLOUD_COMPOSE_CONTAINER_HOME:-/var/lib/lazycloud}"
      LAZYCLOUD_WORKER_CONFIG_PATH: "/etc/lazycloud/worker/worker.yaml"
      LAZYCLOUD_COMPOSE_CONTROL_PLANE_PORT: "${LAZYCLOUD_COMPOSE_CONTROL_PLANE_PORT:-8000}"
      WORKER_REPOSITORY_URL: "http://control-plane:9000"
      GATEWAY_HTTP_URL: "http://control-plane:9000"
      BUILDAH_ISOLATION: "chroot"
      BUILDAH_LAYERS: "true"
      REGISTRY_AUTH_FILE: "/var/lib/lazycloud/registry/auth.json"
      WORKER_ID: "${LAZYCLOUD_COMPOSE_CONTAINER_WORKER_ID:-compose-container-worker}"
      WORKER_MACHINE: "${LAZYCLOUD_COMPOSE_CONTAINER_WORKER_MACHINE:-compose-machine}"
      WORKER_POOL: "${LAZYCLOUD_COMPOSE_CONTAINER_WORKER_POOL:-lazycloud}"
      WORKER_SOURCE_CACHE_STORAGE_ID: "machine:${LAZYCLOUD_COMPOSE_CONTAINER_WORKER_MACHINE:-compose-machine}"
      WORKER_POD_ADDRESS: "${LAZYCLOUD_COMPOSE_CONTAINER_WORKER_POD_ADDRESS:-container-worker}"
      WORKER_CONTAINER_SERVICE_PORT: "${LAZYCLOUD_COMPOSE_CONTAINER_WORKER_SERVICE_PORT:-8910}"
      WORKER_NETWORK_PREFIX: "${LAZYCLOUD_COMPOSE_CONTAINER_WORKER_NETWORK_PREFIX:-compose-shared}"
      WORKER_CHECKPOINT_BUCKET: "lazycloud-objects"
      DATA_STORAGE_BUCKET: "lazycloud-data"
      DATA_STORAGE_ENDPOINT_URL: "http://object-store:9000"
      DATA_STORAGE_REGION_NAME: "us-east-1"
      DATA_STORAGE_ACCESS_KEY_ID: "lazycloud-local"
      DATA_STORAGE_SECRET_ACCESS_KEY: "${DATA_STORAGE_SECRET_ACCESS_KEY:-lazycloud-local-secret}"
      DATA_STORAGE_FORCE_PATH_STYLE: "true"
      WORKER_CACHE_ENDPOINT: "http://cache-server:7900"
      LAZYCLOUD_CACHE_SERVICE_TOKEN_FILE: "/var/lib/lazycloud/cache-token/cache-service-token"
    depends_on:
      <<: *lazycloud-role-depends
      platform-unit:
        condition: service_completed_successfully
      worker-token:
        condition: service_completed_successfully
      cache-server:
        condition: service_healthy
    healthcheck: *container-worker-healthcheck
    volumes:
      - lazycloud-shared-worker:/var/lib/lazycloud
      - lazycloud-worker-tokens:/var/lib/lazycloud/tokens:ro
      - lazycloud-cache-token:/var/lib/lazycloud/cache-token:ro
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
      - /var/run/netns:/var/run/netns
    configs:
      - source: container-worker-configuration
        target: /etc/lazycloud/worker/worker.yaml
        mode: 0444

  cache-token:
    image: cache-server:local
    command:
      - python
      - -c
      - "from pathlib import Path; import secrets; p=Path('/var/lib/lazycloud/tokens/cache-service-token'); p.parent.mkdir(parents=True, exist_ok=True); existing=p.read_text(encoding='utf-8').strip() if p.exists() else ''; p.write_text(existing or secrets.token_urlsafe(48), encoding='utf-8'); p.chmod(0o400)"
    volumes:
      - lazycloud-cache-token:/var/lib/lazycloud/tokens

  cache-server:
    image: cache-server:local
    restart: unless-stopped
    build:
      context: .
      dockerfile: docker/Dockerfile.control-plane
      target: cache-server
      args: *lazycloud-build-args
    command:
      [
        "lazycloud-cache-server",
        "--host",
        "0.0.0.0",
        "--port",
        "7900",
        "--root",
        "/var/lib/lazycloud/cache",
      ]
    environment:
      LAZYCLOUD_HOME: "${LAZYCLOUD_COMPOSE_CONTAINER_HOME:-/var/lib/lazycloud}"
      LAZYCLOUD_CACHE_SERVICE_TOKEN: "${LAZYCLOUD_CACHE_SERVICE_TOKEN:-}"
      LAZYCLOUD_CACHE_SERVICE_TOKEN_FILE: "${LAZYCLOUD_CACHE_SERVICE_TOKEN_FILE:-/var/lib/lazycloud/tokens/cache-service-token}"
      LAZYCLOUD_CACHE_SERVICE_PREVIOUS_TOKEN: "${LAZYCLOUD_CACHE_SERVICE_PREVIOUS_TOKEN:-}"
      LAZYCLOUD_CACHE_MAX_CONTENT_BYTES: "21474836480"
      LAZYCLOUD_CACHE_MAX_OBJECT_BYTES: "8589934592"
      LAZYCLOUD_CACHE_MAX_METADATA_ENTRIES: "100000"
      LAZYCLOUD_CACHE_MAX_PATH_BYTES: "4096"
      LAZYCLOUD_CACHE_DISK_MAX_USAGE_PCT: "0.90"
      LAZYCLOUD_CACHE_DISK_EVICT_WATERMARK_PCT: "0.85"
    depends_on:
      <<: *lazycloud-role-depends
      cache-token:
        condition: service_completed_successfully
    volumes:
      - lazycloud-files:/var/lib/lazycloud
      - lazycloud-cache-token:/var/lib/lazycloud/tokens:ro
      - lazycloud-cache:/var/lib/lazycloud/cache
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import socket; socket.create_connection(('127.0.0.1', 7900), timeout=3).close()",
        ]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 5s

configs:
  pangolin-configuration:
    content: |
      app:
        dashboard_url: "https://pangolin.lazycloud.localhost:8443"
        log_level: "info"
        telemetry:
          anonymous_usage: false
      domains:
        domain1:
          base_domain: "lazycloud.localhost"
      server:
        internal_hostname: "pangolin"
        cors:
          origins: ["https://pangolin.lazycloud.localhost:8443"]
          methods: ["GET", "POST", "PUT", "DELETE", "PATCH"]
          allowed_headers: ["X-CSRF-Token", "Content-Type"]
          credentials: false
      gerbil:
        start_port: 51820
        clients_start_port: 21820
        base_endpoint: "gerbil"
      flags:
        require_email_verification: false
        disable_signup_without_invite: true
        disable_user_create_org: false
        allow_raw_resources: true
        enable_integration_api: true
      traefik:
        http_entrypoint: "web"
        https_entrypoint: "websecure"
        cert_resolver: ""
        prefer_wildcard_cert: false

  pangolin-traefik-configuration:
    content: |
      api:
        insecure: true
        dashboard: true
      providers:
        http:
          endpoint: "http://pangolin:3001/api/v1/traefik-config"
          pollInterval: "5s"
        file:
          filename: "/etc/traefik/dynamic_config.yml"
      experimental:
        plugins:
          badger:
            moduleName: "github.com/fosrl/badger"
            version: "v1.4.0"
      log:
        level: "INFO"
        format: "common"
      entryPoints:
        web:
          address: ":80"
        websecure:
          address: ":443"
          transport:
            respondingTimeouts:
              readTimeout: "30m"
          http3:
            advertisedPort: 443
          http:
            tls: {}
            encodedCharacters:
              allowEncodedSlash: true
              allowEncodedQuestionMark: true
      serversTransport:
        insecureSkipVerify: true
      ping:
        entryPoint: "web"

  pangolin-traefik-dynamic-configuration:
    content: |
      http:
        middlewares:
          badger:
            plugin:
              badger:
                disableForwardAuth: true
          redirect-to-https:
            redirectScheme:
              scheme: https
              port: "8443"
        routers:
          pangolin-dashboard-redirect:
            rule: "Host(`pangolin.lazycloud.localhost`)"
            service: pangolin-dashboard
            entryPoints: [web]
            middlewares: [redirect-to-https, badger]
          pangolin-dashboard:
            rule: "Host(`pangolin.lazycloud.localhost`) && !PathPrefix(`/api/v1`)"
            service: pangolin-dashboard
            entryPoints: [websecure]
            middlewares: [badger]
            tls: {}
          pangolin-api:
            rule: "Host(`pangolin.lazycloud.localhost`) && PathPrefix(`/api/v1`)"
            service: pangolin-api
            entryPoints: [websecure]
            middlewares: [badger]
            tls: {}
          pangolin-websocket:
            rule: "Host(`pangolin.lazycloud.localhost`)"
            service: pangolin-api
            entryPoints: [websecure]
            middlewares: [badger]
            tls: {}
        services:
          pangolin-dashboard:
            loadBalancer:
              servers:
                - url: "http://pangolin:3002"
          pangolin-api:
            loadBalancer:
              servers:
                - url: "http://pangolin:3000"
      tcp:
        serversTransports:
          pp-transport-v1:
            proxyProtocol:
              version: 1
          pp-transport-v2:
            proxyProtocol:
              version: 2

  container-worker-configuration:
    content: |
      configuration:
        execution:
          runtime: runsc
          runtimes: [runsc]
          capacity:
            cpu_millicores: ${LAZYCLOUD_COMPOSE_CONTAINER_WORKER_CPU_MILLICORES:-4000}
            memory_mib: ${LAZYCLOUD_COMPOSE_CONTAINER_WORKER_MEMORY_MIB:-8192}
            gpu_type: ""
            gpu_count: 0
          pool_mode: public
          billing_owner: platform_fleet
          preemptible: false
          persistent: true
          agent_worker: true
        network:
          route_transport: direct
          agent_bridge_network: true
          bridge_name: ${LAZYCLOUD_COMPOSE_SHARED_WORKER_BRIDGE:-rt_br2}
          bridge_subnet: ${LAZYCLOUD_COMPOSE_SHARED_WORKER_SUBNET:-192.168.32.0/20}
          bridge_ipv6_subnet: ${LAZYCLOUD_COMPOSE_SHARED_WORKER_IPV6_SUBNET:-fd00:abcf::/64}
        paths:
          bundle_root: /var/lib/lazycloud/bundles
          image_cache_path: /var/lib/lazycloud/images
          image_mount_root: /var/lib/lazycloud/image-mounts
          image_build_root: /var/lib/lazycloud/builds
          source_cache_root: /var/lib/lazycloud/source-cache
          checkpoint_root: /var/lib/lazycloud/checkpoints
        monitoring:
          metrics_enabled: true
          metrics_interval_seconds: 5

volumes:
  postgres-data:
  pangolin-postgres-data:
  pangolin-gerbil-config:
  pangolin-runtime:
  redis-data:
  object-store-data:
  lazycloud-files:
  lazycloud-administrator-token:
  lazycloud-cache-token:
  lazycloud-cache:
  lazycloud-worker-tokens:
  lazycloud-shared-worker:
  tcp-ingress-tls:

secrets:
  lazycloud-bootstrap-administrator-token:
    environment: LAZYCLOUD_TOKEN
