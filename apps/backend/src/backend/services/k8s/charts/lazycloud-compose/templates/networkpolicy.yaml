{{/*
Create NetworkPolicies to enforce network isolation between services
Note: The namespace-level default deny policy is handled by the lazycloud-namespace chart
*/}}
{{- if .Values.networkPolicies.enabled }}
{{/* Per-service network policies */}}
{{- range $service := .Values.services }}
{{- if $service.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ $service.name }}
  namespace: {{ include "lazycloud-compose.serviceNamespace" (dict "serviceName" $service.name "Values" $.Values "root" $) }}
  labels:
    {{- include "lazycloud-compose.labels" $ | nindent 4 }}
    app.kubernetes.io/name: {{ $service.name }}
    app.kubernetes.io/component: network-policy
    lazycloud.dev/service: {{ $service.name }}
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: {{ $service.name }}
      app.kubernetes.io/instance: {{ $.Release.Name }}
  policyTypes:
  - Ingress
  - Egress
  
  ingress:
  {{- if $service.networks }}
  # Allow traffic from services in the same network(s) and workspace
  {{- range $network := $service.networks }}
  - from:
    - namespaceSelector:
        matchLabels:
          lazycloud.dev/workspace-id: {{ $.Values.global.workspaceId | quote }}
      podSelector:
        matchLabels:
          lazycloud.dev/network.{{ $network.name }}: "true"
  {{- end }}
  {{- end }}
  
  # Allow traffic from ingress controller if service has ingress
  {{- if $service.ingress }}
  {{- if $service.ingress.enabled }}
  - from:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: {{ $.Values.networkPolicies.ingressNamespace | default "ingress-nginx" }}
  {{- end }}
  {{- end }}
  
  # Allow metrics scraping if enabled
  {{- if $service.metrics }}
  {{- if $service.metrics.enabled }}
  - from:
    - namespaceSelector:
        matchLabels:
          name: {{ $.Values.networkPolicies.monitoringNamespace | default "monitoring" }}
    ports:
    - protocol: TCP
      port: {{ $service.metrics.port }}
  {{- end }}
  {{- end }}
  
  egress:
  # Allow DNS
  - to:
    - namespaceSelector: {}
    ports:
    - protocol: TCP
      port: 53
    - protocol: UDP
      port: 53
  
  # Allow traffic to services in the same network(s) and workspace
  {{- range $network := $service.networks }}
  - to:
    - namespaceSelector:
        matchLabels:
          lazycloud.dev/workspace-id: {{ $.Values.global.workspaceId | quote }}
      podSelector:
        matchLabels:
          lazycloud.dev/network.{{ $network.name }}: "true"
  {{- end }}
  
  # Allow external traffic only if explicitly enabled for this service
  {{- if and $.Values.networkPolicies.allowExternal $service.allowExternal }}
  - to:
    - ipBlock:
        cidr: 0.0.0.0/0
        except:
        - 169.254.169.254/32  # Cloud metadata service (AWS, Hetzner, etc.)
  {{- end }}
{{- end }}
{{- end }}
{{- end }}