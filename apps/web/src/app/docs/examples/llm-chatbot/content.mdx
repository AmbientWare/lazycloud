# LLM Chatbot

A full-stack AI chatbot showing how LazyCloud handles multi-service apps with persistent storage, resource limits, and secrets.

<Tip>
**What you'll learn:** Multi-service orchestration, persistent volumes for databases, Redis caching, and encrypted secret handling.
</Tip>

```
┌──────────┐     ┌──────────┐     ┌──────────┐
│ Next.js  │────▶│ FastAPI  │────▶│  OpenAI  │
│ Frontend │     │   API    │     │          │
└──────────┘     └────┬─────┘     └──────────┘
                      │
              ┌───────┴───────┐
              ▼               ▼
         ┌────────┐     ┌─────────┐
         │ SQLite │     │  Redis  │
         └────────┘     └─────────┘
```

## Run it

```bash
git clone https://github.com/AmbientWare/lazycloud-releases.git
cd lazycloud-releases/examples/llm-chatbot

# Local
export OPENAI_API_KEY=sk-...
docker compose up --build

# Deploy
lazycloud init
lazycloud deploy
```

## What's configured

This example uses standard Docker Compose with a few LazyCloud labels.

### Environment variables

```yaml
environment:
  - OPENAI_API_KEY=${OPENAI_API_KEY}
  - OPENAI_MODEL=${OPENAI_MODEL:-gpt-4o-mini}
  - REDIS_URL=redis://redis:6379
```

Environment variables defined in your compose file are detected during `lazycloud deploy`. You'll be prompted to import values from a `.env` file, your shell environment, or enter them manually. Values are stored encrypted.

Services can reference each other by name—`redis://redis:6379` resolves to the Redis container automatically.

### Build arguments

```yaml
frontend:
  build:
    context: ./frontend
    args:
      # LazyCloud auto-resolves LC_API_PUBLIC_URL, uses default locally
      NEXT_PUBLIC_API_URL: ${LC_API_PUBLIC_URL:-http://localhost:8000}
```

Build arguments are for values needed at build time. The Next.js frontend uses `NEXT_PUBLIC_API_URL` to know where to send API requests—this gets embedded into the JavaScript bundle during build.

By referencing `${LC_API_PUBLIC_URL:-default}`, LazyCloud automatically resolves the public URL for the API service without prompting. The `:-http://localhost:8000` part provides a fallback for local development with `docker compose up`.

<Tip>
For server-side code, you don't need build args—LazyCloud automatically injects `LC_API_URL` and `LC_API_PUBLIC_URL` at runtime.
</Tip>

### Resource limits

```yaml
deploy:
  resources:
    limits:
      cpus: "1.0"
      memory: 512M
    reservations:
      cpus: "0.25"
      memory: 256M
```

Set CPU and memory per service. `limits` caps max usage, `reservations` guarantees minimum resources.

### Persistent storage

```yaml
volumes:
  api-data:
    labels:
      lazycloud.volume.size: "10Gi"
  redis-data:
    labels:
      lazycloud.volume.size: "5Gi"
```

Data persists across deployments and restarts. Each service gets its own volume—SQLite database in `api-data`, Redis cache in `redis-data`.

---

See [Labels](/docs/labels) for all configuration options.
