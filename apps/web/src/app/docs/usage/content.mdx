# Usage

View resource consumption and billing.

```bash
lazycloud usage
```

Opens an interactive dashboard showing compute hours, storage, and current billing period stats.

Each workspace tracks usage independently. Switch workspaces to view different usage:

```bash
lazycloud workspaces activate my-team
lazycloud usage
```
