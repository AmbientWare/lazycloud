/* ========================================
   CONTAINER COMPONENTS
   ======================================== */

/* Section containers */
SectionContainer {
    border: round $primary;
    padding: 0 1;
    height: auto;
    border-title-color: $accent;
}

Container > .container--title {
    color: $accent;
}
