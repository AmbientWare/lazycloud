from __future__ import annotations

from shared.app_identity import NAME

EXAMPLE_DOMAIN = f"{NAME}.example"
EXAMPLE_URL = f"https://{EXAMPLE_DOMAIN}"
EXAMPLE_COM_DOMAIN = f"{NAME}.example.com"
EXAMPLE_COM_URL = f"https://{EXAMPLE_COM_DOMAIN}"
HTTP_EXAMPLE_COM_URL = f"http://{EXAMPLE_COM_DOMAIN}"
INTERNAL_DOMAIN = f"{NAME}.internal"
INTERNAL_URL = f"https://{INTERNAL_DOMAIN}"
TEST_DOMAIN = f"{NAME}.test"
TEST_URL = f"https://{TEST_DOMAIN}"
TRACE_TESTS_NAME = f"{NAME}.tests"
