# Control Package

Own workspace, app, stub, deployment/resource, route, cron, and concurrency
workflows. Services decide; database repositories persist; apps format. Use
typed domain errors and narrow event/context/storage protocols. Every lookup,
mutation, and registration preserves tenant/owner scope. Public payloads live in
`shared.http`; control/database records do not leak into broad shared models.
