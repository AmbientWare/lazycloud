# Operations Package

Own management workflows genuinely reused by API, CLI, or gateway. Depend on
typed service protocols and explicit repositories, not apps, SDK, runner,
providers, or a broad service graph. Raise typed domain errors and preserve
authority, retry, idempotency, and cleanup through the owning public command or
route.
