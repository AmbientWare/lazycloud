# Foundation Package

Own small, narrowly named protocol-neutral process, network, shell, payload,
validation, header/path, URL, and ID helpers. No workflow decisions, broad
miscellaneous helpers, apps, SDK, persistence clients, services, providers,
schedulers, workers, or composition. Move helpers to their domain owner when one
exists and delete old paths. Accept nontrivial helpers through their consumer.
