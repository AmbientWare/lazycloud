# Cache Package

Own typed deterministic hot coordination and content-addressed runtime data.
Redis coordination is not durable history; object/filesystem responsibilities
remain behind storage protocols. Validate hashes, sizes, completeness, atomic
materialization, corruption, concurrency, and cleanup. Worker startup continues
to own image-archive cache execution.
