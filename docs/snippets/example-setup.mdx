Install the CLI once with `uv tool install lazycloud-client`. The commands
below download a standalone project and install its dependencies with uv. You
do not need a repository checkout. Run the rest of this guide from the project
directory, through `uv run` so commands use the project's pinned SDK.
