Install the CLI once with `uv tool install lazycloud-client`. The commands
below download a standalone project and install its dependencies with uv.
No repository checkout is needed. Run the rest of this guide from that project's
directory, using `uv run` so commands use its pinned SDK.
