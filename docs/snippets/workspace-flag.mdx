<Note>
  Commands that operate on workspace resources accept `--workspace <name>` to
  target a workspace other than the one stored in the active profile. Only
  admin tokens may use it for cross-workspace access.
</Note>
