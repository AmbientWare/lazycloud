<Note>
  Commands that operate on workspace resources accept `--workspace <name>` to
  target a workspace other than the one stored in the active profile. Your
  credential reaches every workspace your account belongs to; only a platform
  administrator can target one they are not a member of.
</Note>
