---
title: "Use the dashboard"
description: "Find run results, test an API, manage stored files, and check your spending."
---

Open [the dashboard](https://lazycloud.dev/dashboard) and choose your workspace
from the menu at the top.

## Find a result or investigate a failure

Open Tasks and select the run. Its detail view shows status, timing, retries,
the result or error, and logs. For a task that is still pending, check the
displayed reason before retrying it.

You can also open an app to find its workloads and recent activity.

## Test or stop a deployed service

Open the workload under Apps and use the request playground to send input.
Use its controls to scale, pause, resume, or delete it. Pausing is useful when
you want to stop serving without removing the definition.

For a running sandbox, open its terminal, file browser, or process list from
the app page.

## Retrieve files

Storage contains volumes, artifacts, secrets, queues, and maps. Use the
volume browser for shared files and the artifact browser for task output.
Artifact downloads and public URLs are subject to
[retention](/concepts/artifacts#retention).

## Check spending

Usage shows account spending by app and workload. The account total is the
same across workspaces you own.

Open Settings, then Billing to check your balance, payment method, plan, and
monthly usage limit. See [Plans and limits](/platform/plans) for what happens
when a limit is reached.

## Manage access and compute

In Settings, Tokens creates access tokens for scripts and direct HTTP calls.
Compute connects an AWS account or joins a machine.
Domains registers a hostname for your API.

Use the workspace menu to create or rename a workspace and manage members.
Check which account you are signed in as before opening an invitation link;
accepting joins that account to the workspace.
