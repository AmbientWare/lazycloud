---
title: "CLI overview"
description: "The lazycloud command: global flags, handler references, and command groups."
---

The `lazycloud` command installs with the `lazycloud` Python package. See
[Installation](/getting-started/installation). Sign in once with
`lazycloud login`; see [Sign-in and workspaces](/platform/auth).

```text
lazycloud [--json] [--debug] COMMAND [ARGS]...
```

`--json` prints one JSON document to stdout for scripting; progress goes to
stderr and errors become one JSON error document with a nonzero exit code.
Log-follow commands print newline-delimited JSON, one record per event.
Interactive commands (`shell`, `dev`, `serve`) reject `--json`. `--debug`
shows full tracebacks. `-h` or `--help` works on every command.

Human output is the default. Styling is dropped when output is redirected.

## Handler references

Commands that take a `HANDLER` accept `<file-or-module>:<object>`:

- `quickstart.py:hello`, a Python file and a decorated object in it.
- `myproject.api:app`, an importable module and an object in it.

The object is one decorated resource or an `App`. Deploying an `App` deploys
everything it registers; `--resource <name>` or `--resource <kind>:<name>`
selects one.

`deploy` also accepts a file or module without `:object`, such as
`lazycloud deploy quickstart.py`, to deploy every decorated function defined there.

## Command groups

| Commands | Reference |
| --- | --- |
| `deploy`, `run`, `serve`, `shell`, `dev` | [Deploy and run](/cli/deploy-and-run) |
| `task`, `logs` | [Tasks and logs](/cli/tasks-and-logs) |
| `app`, `deployment`, `container`, `client` | [Apps and deployments](/cli/deployments-and-containers) |
| `volume`, `ls`, `cp`, `rm`, `mv`, `secret` | [Storage](/cli/storage) |
| `compute`, `cloud`, `machine`, `domain` | [Compute and domains](/cli/compute) |
| `login`, `workspace`, `profile`, `token` | [Sign-in](/cli/auth) |
| `quickstart`, `create-app`, `example` | [Scaffolding](/cli/scaffolding) |
