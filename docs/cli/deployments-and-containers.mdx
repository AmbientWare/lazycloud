---
title: "app, deployment, container"
description: "Manage deployed apps, deployments, and running containers."
---

import WorkspaceFlag from '/snippets/workspace-flag.mdx';

Pause an app to stop its workloads together. Stop one deployment to take a
single service offline. Container commands act on individual instances;
an active deployment may replace a container you stop.

## lazycloud app

An app is addressed by name or id.

The blocks below show command syntax. Replace uppercase names and omit
square brackets when supplying an optional flag.

```bash
lazycloud app list [--active | --inactive | --all]
lazycloud app show APP
lazycloud app export APP    # write a typed Python package for its deployed workloads
lazycloud app pause APP      # stop every workload in the app
lazycloud app resume APP
lazycloud app delete APP
```

## lazycloud deployment

A deployment is addressed by name or id. `scale` sets the replica count for a Pod.
Functions and endpoints scale from their autoscaler configuration.

Use these commands for inspection and operational changes. Keep workload
resources and autoscaler settings in Python so a later deploy uses your intended
configuration.

```bash
lazycloud deployment list [--app <name>] [--limit <n>]
lazycloud deployment stop IDS_OR_NAMES...        # stop serving; keep the definition
lazycloud deployment start ID_OR_NAME
lazycloud deployment scale ID_OR_NAME --containers <n>
lazycloud deployment delete ID_OR_NAME
```

```bash
lazycloud deployment list --app tools
lazycloud deployment scale web --containers 3
```

## lazycloud container

```bash
lazycloud container list [--limit <n>]                        # recent containers, default 100
lazycloud container attach CONTAINER_ID                       # stream output until it exits
lazycloud container checkpoint CONTAINER_ID [--checkpoint-id <id>]
lazycloud container stop CONTAINER_IDS...
```

<WorkspaceFlag />
