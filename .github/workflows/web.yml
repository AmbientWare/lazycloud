name: Web owner

on:
  pull_request:
    paths:
      - ".github/workflows/web.yml"
      - "apps/web/**"
      - "tests/contracts/http_contract_cases.json"
  push:
    branches:
      - main
    paths:
      - ".github/workflows/web.yml"
      - "apps/web/**"
      - "tests/contracts/http_contract_cases.json"

permissions:
  contents: read

concurrency:
  group: web-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  validate:
    name: Web checkpoint
    runs-on: ubuntu-26.04
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      # Vitest runs under Node; pinning it keeps the jsdom Web Storage behaviour the
      # tests rely on from moving with the runner image.
      - name: Install Node
        uses: actions/setup-node@v7
        with:
          node-version: "24"

      - name: Install Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.12"

      - name: Install dependencies
        working-directory: apps/web
        run: bun install --frozen-lockfile

      - name: Validate web owner
        working-directory: apps/web
        run: |
          bun run format:check
          bun run lint
          bun run typecheck
          bun run test --reporter=default --reporter=junit --outputFile=test-results/vitest.xml
          bun run build

      - name: Upload test timings
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: web-test-results
          path: apps/web/test-results/vitest.xml
          retention-days: 14
