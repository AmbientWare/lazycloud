{{/*
Negative sync waves finish schema, identity and billing bootstrap before the
control plane starts. Database consumers run in separate waves, so the database
budget includes one bootstrap Job at a time.

The administrator Job requires LAZYCLOUD_TOKEN from the projected secret. Token
generation requires an explicit output path and is reserved for offline use.
Recover an existing installation through offline administrator recovery.
*/}}
apiVersion: batch/v1
kind: Job
metadata:
  name: database-bootstrap
  annotations:
    # A wave orders this, not a hook. Every PreSync hook runs ahead of every
    # normal wave, the ExternalSecret among them, so a hook here asks for secret
    # keys the Secret does not carry until wave -4 and the sync wedges on a
    # container that cannot be created. Adding a credential is what triggers it,
    # which is the moment the schema change needing one is being deployed.
    # The wave already gives migrations the ordering they need: Argo holds -3
    # until this Job completes, so nothing reading the schema is applied while
    # it runs, and a failure stops the sync with the workloads untouched.
    argocd.argoproj.io/sync-wave: "-3"
    # Deleted and recreated rather than patched or replaced in place. A Job's
    # pod template is immutable, so the image tag moving with the commit makes
    # every later sync fail to apply this one, and the wave it is in never
    # completes: the release stops at a Job that already ran and reports an
    # immutable field rather than anything about the deploy.
    #
    # `Force` is what makes it a delete, and it is not optional here. `Replace`
    # alone sends the rendered manifest as it stands, and Kubernetes generates a
    # Job's selector and its pod labels at creation, so what is sent back is
    # missing both and is refused for a selector that does not match a template
    # it also does not have.
    #
    # Re-running is safe because each of these is idempotent, and for three of
    # them running every time is the point.
    argocd.argoproj.io/sync-options: Force=true,Replace=true
spec:
  backoffLimit: 3
  template:
    metadata:
      name: database-bootstrap
    spec:
      restartPolicy: Never
      serviceAccountName: {{ .Values.serviceAccounts.controlPlane }}
      containers:
        - name: database-bootstrap
          image: {{ include "lazycloud.image" (list . "database-bootstrap") }}
          command: ["lazycloud-admin", "database", "migrate"]
          env:
            {{- include "lazycloud.env" (list . "databaseBootstrap") | nindent 12 }}
            {{- include "lazycloud.databaseEnv" .Values.bootstrap.database | nindent 12 }}
          resources:
            {{- toYaml .Values.bootstrap.resources | nindent 12 }}
---
apiVersion: batch/v1
kind: Job
metadata:
  name: administrator-bootstrap
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
    # Deleted and recreated rather than patched or replaced in place. A Job's
    # pod template is immutable, so the image tag moving with the commit makes
    # every later sync fail to apply this one, and the wave it is in never
    # completes: the release stops at a Job that already ran and reports an
    # immutable field rather than anything about the deploy.
    #
    # `Force` is what makes it a delete, and it is not optional here. `Replace`
    # alone sends the rendered manifest as it stands, and Kubernetes generates a
    # Job's selector and its pod labels at creation, so what is sent back is
    # missing both and is refused for a selector that does not match a template
    # it also does not have.
    #
    # Re-running is safe because each of these is idempotent, and for three of
    # them running every time is the point.
    argocd.argoproj.io/sync-options: Force=true,Replace=true
spec:
  backoffLimit: 3
  template:
    metadata:
      name: administrator-bootstrap
    spec:
      restartPolicy: Never
      serviceAccountName: {{ .Values.serviceAccounts.controlPlane }}
      containers:
        - name: administrator-bootstrap
          image: {{ include "lazycloud.image" (list . "database-bootstrap") }}
          command: ["lazycloud-admin", "--json", "auth", "bootstrap"]
          env:
            {{- include "lazycloud.env" (list . "administratorBootstrap") | nindent 12 }}
            {{- include "lazycloud.databaseEnv" .Values.bootstrap.database | nindent 12 }}
          resources:
            {{- toYaml .Values.bootstrap.resources | nindent 12 }}
---
{{/*
The catalog, every sync rather than once.

Signing in provisions a subscription on the free plan and fails closed when it
cannot, and a subscription resolves its prices by lookup key. So an account whose
catalog is unpublished refuses every sign-in it receives, and a plan shipped
without its price refuses everyone the code puts on it. Running this only at
install would leave the second case to be discovered by a person who could not
sign in.

Additive and idempotent by name: a run against a published account creates
nothing. What it will not do is edit. A price whose amount disagrees with this
repository is a conflict and fails the Job, because a published price is what
customers are already billed against and repointing one is a decision with an
invoice attached, not a step of a deploy. The deploy stops there rather than
serving a rate card the provider does not agree with.

`--confirm-account` comes from the deployment rather than from the credential.
A key does not say which account it belongs to, so one swapped for another
account's is refused here instead of quietly publishing this catalog somewhere
else.
*/}}
apiVersion: batch/v1
kind: Job
metadata:
  name: billing-catalog
  annotations:
    argocd.argoproj.io/sync-wave: "-3"
    # Deleted and recreated rather than patched or replaced in place. A Job's
    # pod template is immutable, so the image tag moving with the commit makes
    # every later sync fail to apply this one, and the wave it is in never
    # completes: the release stops at a Job that already ran and reports an
    # immutable field rather than anything about the deploy.
    #
    # `Force` is what makes it a delete, and it is not optional here. `Replace`
    # alone sends the rendered manifest as it stands, and Kubernetes generates a
    # Job's selector and its pod labels at creation, so what is sent back is
    # missing both and is refused for a selector that does not match a template
    # it also does not have.
    #
    # Re-running is safe because each of these is idempotent, and for three of
    # them running every time is the point.
    argocd.argoproj.io/sync-options: Force=true,Replace=true
spec:
  backoffLimit: 3
  template:
    metadata:
      name: billing-catalog
    spec:
      restartPolicy: Never
      serviceAccountName: {{ .Values.serviceAccounts.controlPlane }}
      containers:
        - name: billing-catalog
          image: {{ include "lazycloud.image" (list . "database-bootstrap") }}
          command:
            - lazycloud-admin
            - --json
            - billing
            - publish-catalog
            - --confirm-account
            - {{ required "runtime.LAZYCLOUD_STRIPE_ACCOUNT_ID is required" .Values.runtime.LAZYCLOUD_STRIPE_ACCOUNT_ID | quote }}
            - --confirm
          env:
            {{- include "lazycloud.env" (list . "billingCatalog") | nindent 12 }}
            {{- include "lazycloud.databaseEnv" .Values.bootstrap.database | nindent 12 }}
          resources:
            {{- toYaml .Values.bootstrap.resources | nindent 12 }}
---
{{/*
Publish the code-owned rate history after schema migration and before serving
billable work. Repeated publication verifies existing rows; conflicting prices
fail instead of rewriting rates already used for customer charges.
*/}}
apiVersion: batch/v1
kind: Job
metadata:
  name: billing-rates
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
    # Deleted and recreated rather than patched or replaced in place, for the
    # reason the Jobs above state at length: a Job's pod template is immutable,
    # the image tag moves with the commit, and `Replace` without `Force` sends
    # back a manifest missing the selector Kubernetes generated.
    argocd.argoproj.io/sync-options: Force=true,Replace=true
spec:
  backoffLimit: 3
  template:
    metadata:
      name: billing-rates
    spec:
      restartPolicy: Never
      serviceAccountName: {{ .Values.serviceAccounts.controlPlane }}
      containers:
        - name: billing-rates
          image: {{ include "lazycloud.image" (list . "database-bootstrap") }}
          command:
            - lazycloud-admin
            - --json
            - billing
            - publish-rates
            - --confirm
          env:
            {{- include "lazycloud.env" (list . "billingRates") | nindent 12 }}
            {{- include "lazycloud.databaseEnv" .Values.bootstrap.database | nindent 12 }}
          resources:
            {{- toYaml .Values.bootstrap.resources | nindent 12 }}
