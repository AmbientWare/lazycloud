apiVersion: v1
kind: ConfigMap
metadata:
  name: active-release
  annotations:
    argocd.argoproj.io/hook: PostSync
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation
data:
  active.json: {{ required "release.active is required" .Values.release.active | toJson | quote }}
