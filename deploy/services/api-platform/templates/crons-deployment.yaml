apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Chart.Name }}-crons
  labels:
    app: {{ .Chart.Name }}-crons
    component: cron
spec:
  replicas: {{ .Values.crons.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Chart.Name }}-crons
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}-crons
        component: cron
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ .Chart.Name }}
      containers:
        - name: cron-worker
          image: "{{ .Values.image.registry }}:{{ .Values.crons.image.name }}-{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["uv", "run", "--no-sync", "python", "-m", "backend.worker"]
          env:
          {{- range .Values.secrets.env }}
            - name: {{ . }}
              valueFrom:
                secretKeyRef:
                  name: {{ $.Values.secrets.name }}
                  key: {{ . }}
          {{- end }}
          volumeMounts:
            - name: shared-storage
              mountPath: /shared-storage
          livenessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - pgrep -f 'backend.worker' || exit 1
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            limits:
              cpu: {{ .Values.crons.resources.limits.cpu }}
              memory: {{ .Values.crons.resources.limits.memory }}
            requests:
              cpu: {{ .Values.crons.resources.requests.cpu }}
              memory: {{ .Values.crons.resources.requests.memory }}
      volumes:
        - name: shared-storage
          persistentVolumeClaim:
            claimName: {{ .Chart.Name }}-shared-storage
