# Cluster-specific values for ash-1 cluster
# ash-1 is the primary cluster with route_root_domain=true in Terraform

ingress:
  # Root domain - ash-1 handles lazycloud.dev and *.lazycloud.dev
  hostname: lazycloud.dev
