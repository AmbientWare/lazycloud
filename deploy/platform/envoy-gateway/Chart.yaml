apiVersion: v2
name: envoy-gateway
description: Envoy Gateway for routing traffic via Gateway API (replacing nginx-ingress)
type: application
version: 0.1.0
appVersion: "1.2.0"
dependencies:
  - name: gateway-helm
    version: "1.2.0"
    repository: oci://docker.io/envoyproxy
    alias: envoy-gateway
