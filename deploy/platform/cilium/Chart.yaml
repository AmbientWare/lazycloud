apiVersion: v2
name: cilium
description: Cilium CNI for Hetzner Cloud (replaces Flannel + kube-proxy)
type: application
version: 0.1.0
appVersion: "1.16.5"
dependencies:
  - name: cilium
    version: "1.16.5"
    repository: https://helm.cilium.io/
