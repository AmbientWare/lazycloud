# Prometheus Stack values
# No infrastructure values needed - static configuration only

kube-prometheus-stack:
  prometheus:
    prometheusSpec:
      retention: 7d
      resources:
        limits:
          cpu: 1000m
          memory: 2Gi
        requests:
          cpu: 500m
          memory: 1Gi
      storageSpec:
        volumeClaimTemplate:
          spec:
            accessModes: ["ReadWriteOnce"]
            resources:
              requests:
                storage: 50Gi

  alertmanager:
    enabled: true
    alertmanagerSpec:
      resources:
        limits:
          cpu: 100m
          memory: 128Mi
        requests:
          cpu: 50m
          memory: 64Mi

  grafana:
    enabled: true
    adminPassword: "admin"  # Change in production via External Secret
    resources:
      limits:
        cpu: 200m
        memory: 256Mi
      requests:
        cpu: 100m
        memory: 128Mi
    additionalDataSources:
      - name: Loki
        type: loki
        url: http://loki-stack:3100
        access: proxy
        isDefault: false

  kubeStateMetrics:
    enabled: true

  nodeExporter:
    enabled: true
