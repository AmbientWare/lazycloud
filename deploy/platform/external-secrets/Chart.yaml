apiVersion: v2
name: external-secrets
description: External Secrets Operator with AWS Secrets Manager (Hetzner)
type: application
version: 0.1.0
appVersion: "0.18.0"
dependencies:
  - name: external-secrets
    version: "0.18.0"
    repository: https://charts.external-secrets.io
    # Operator installed by Terraform; disable here to avoid conflict
    condition: operator.enabled
